import React from 'react'

const Dashboard = () => {
    return (
        <div style={{ display: 'flex', alignItems: 'start', justifyContent: 'center', height: '100vh', color: '#000' }}>dashboard</div>
    )
}

export default Dashboard